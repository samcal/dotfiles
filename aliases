# Unix
alias ln='ln -v'
alias mkdir='mkdir -p'
alias ...='../..'
alias l='exa'
alias ll='exa -al'
alias lll='exa --tree --long'

# PostgreSQL
alias proddb='psql "$PRODUCTION_FOLLOWER_DATABASE_URL"'
alias stgdb='psql "$STAGING_DATABASE_URL"'
alias locdb='psql "$LOCAL_DATABASE_URL"'
